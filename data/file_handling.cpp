#include <iostream>
#include <fstream>
using namespace std;

int main()
{
    // Opeaning and writing files using constructor
    // string st = "Hello World";
    // ofstream out("sample.txt");
    // out << st;

    // Opening and reading files using constructor
    // string st2;
    // ifstream in("sample.txt");
    // in >> st2;
    // cout << st2;
    return 0;

    // NOTE: using ofstream and ifstream for same file won't work as file will be opened in which ever comes first
}